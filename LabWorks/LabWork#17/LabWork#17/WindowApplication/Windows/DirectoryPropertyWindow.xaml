<Window x:Class="WindowApplication.Windows.DirectoryPropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WindowApplication.Windows"
        mc:Ignorable="d"
        Title="DirectoryPropertyWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <TextBlock Name="DirectoryNameTextBlock"/>
        <TextBlock Name="FilesCountTextBlock" Grid.Row="1"/>
        <TextBlock Name="DirectoryCountTextBlock" Grid.Row="2"/>
        <TextBlock Name="SizeTextBlock" Grid.Row="3"/>
        <TextBlock Name="ProcentOccupatedTextBlock" Grid.Row="4"/>

        <StackPanel Grid.Row="5">
            <Label Content="Топ 5 больших файлов"/>
            <ListView Name="TopFilesList" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Path=Length}" Width="200">Размер в Б</GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Path=FullName}" Width="200">Название</GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
        

    </Grid>
</Window>
